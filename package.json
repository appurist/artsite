{
  "name": "artsite-appwrite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "pnpm build && cd workers && wrangler deploy",
    "deploy:dev": "pnpm build && cd workers && wrangler deploy --env development",
    "upload": "wrangler versions upload",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.1.2",
    "wrangler": "^4.35.0"
  },
  "dependencies": {}
}

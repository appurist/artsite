{
  "name": "artsite-appwrite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:backend": "cd workers && wrangler dev",
    "build": "vite build",
    "deploy:prod": "pnpm build && cd workers && wrangler deploy --env production",
    "deploy:dev": "pnpm build && cd workers && wrangler deploy --env development",
    "upload": "wrangler versions upload",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.1.2",
    "wrangler": "^4.36.0"
  },
  "dependencies": {}
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= config.pages.contact.title %> - <%= config.site.title %></title>
    <meta name="description" content="<%= config.site.description %>">
    <link rel="stylesheet" href="/css/gallery.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Source+Sans+Pro:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: <%= config.site.theme.primaryColor %>;
            --secondary-color: <%= config.site.theme.secondaryColor %>;
        }
    </style>
</head>
<body>
    <nav class="main-nav">
        <div class="nav-content">
            <a href="/" class="logo"><%= config.site.title %></a>
            <ul class="nav-menu">
                <li><a href="/">Gallery</a></li>
                <% if (config.pages.about.enabled) { %>
                    <li><a href="/about">About</a></li>
                <% } %>
                <% if (config.pages.contact.enabled) { %>
                    <li><a href="/contact" class="active">Contact</a></li>
                <% } %>
                <% if (config.pages.blog.enabled) { %>
                    <li><a href="/blog">Blog</a></li>
                <% } %>
            </ul>
        </div>
    </nav>

    <main class="page-main">
        <div class="page-container">
            <header class="page-header">
                <h1><%= config.pages.contact.title %></h1>
            </header>
            
            <div class="page-content">
                <% if (config.pages.contact.content) { %>
                    <section class="page-text">
                        <div class="content-text">
                            <%- config.pages.contact.content.replace(/\n/g, '<br>') %>
                        </div>
                    </section>
                <% } %>
                
                <section class="contact-info">
                    <div class="contact-grid">
                        <% if (config.artist.email) { %>
                            <div class="contact-item">
                                <h3>Email</h3>
                                <p><a href="mailto:<%= config.artist.email %>"><%= config.artist.email %></a></p>
                            </div>
                        <% } %>
                        
                        <% if (config.artist.phone) { %>
                            <div class="contact-item">
                                <h3>Phone</h3>
                                <p><%= config.artist.phone %></p>
                            </div>
                        <% } %>
                        
                        <% if (config.artist.website) { %>
                            <div class="contact-item">
                                <h3>Website</h3>
                                <p><a href="<%= config.artist.website %>" target="_blank"><%= config.artist.website %></a></p>
                            </div>
                        <% } %>
                    </div>
                </section>
                
                <section class="inquiry-info">
                    <h3>Inquiries</h3>
                    <p>Interested in purchasing artwork or commissioning a piece? <% if (config.artist.email) { %>Please <a href="mailto:<%= config.artist.email %>">send an email</a> with details about your interest.<% } else { %>Please get in touch using the contact information above.<% } %></p>
                </section>
            </div>
        </div>
    </main>

    <footer class="gallery-footer">
        <p>&copy; <%= new Date().getFullYear() %> <%= config.site.title %>. All rights reserved.</p>
    </footer>
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= config.site.title %></title>
    <meta name="description" content="<%= config.site.description %>">
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="apple-touch-icon" href="/apple-touch-icon.png">
    <link rel="stylesheet" href="/css/gallery.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Source+Sans+Pro:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: <%= config.site.theme.primaryColor %>;
            --secondary-color: <%= config.site.theme.secondaryColor %>;
        }
    </style>
</head>
<body>
    <nav class="main-nav">
        <div class="nav-content">
            <a href="/" class="logo"><%= config.site.title %></a>
            <ul class="nav-menu">
                <li><a href="/">Gallery</a></li>
                <% if (config.pages.about.enabled) { %>
                    <li><a href="/about">About</a></li>
                <% } %>
                <% if (config.pages.contact.enabled) { %>
                    <li><a href="/contact">Contact</a></li>
                <% } %>
                <% if (config.pages.blog.enabled) { %>
                    <li><a href="/blog">Blog</a></li>
                <% } %>
            </ul>
        </div>
    </nav>

    <header class="gallery-header">
        <div class="header-content">
            <h1 class="gallery-title"><%= config.site.title %></h1>
            <p class="gallery-subtitle"><%= config.site.subtitle %></p>
        </div>
    </header>

    <main class="gallery-main">
        <% if (artworks.length === 0) { %>
            <div class="empty-gallery">
                <h2>Gallery Coming Soon</h2>
                <p>New artwork will be added regularly. Please check back soon!</p>
            </div>
        <% } else { %>
            <div class="artwork-count">
                <p><%= artworks.length %> artwork<%= artworks.length !== 1 ? 's' : '' %> in collection</p>
            </div>
            
            <div class="gallery-grid">
                <% artworks.forEach(function(artwork) { %>
                    <article class="artwork-item">
                        <a href="/artwork/<%= artwork.id %>" class="artwork-link">
                            <div class="artwork-image-container">
                                <img src="/uploads/thumbs/<%= artwork.thumbnail_filename %>" 
                                     alt="<%= artwork.title %>" 
                                     class="artwork-image"
                                     loading="lazy">
                                <div class="artwork-overlay">
                                    <span class="view-artwork">View Details</span>
                                </div>
                            </div>
                            <div class="artwork-details">
                                <h2 class="artwork-title"><%= artwork.title %></h2>
                                <% if (artwork.medium) { %>
                                    <p class="artwork-medium"><%= artwork.medium %></p>
                                <% } %>
                                <% if (artwork.year_created) { %>
                                    <p class="artwork-year"><%= artwork.year_created %></p>
                                <% } %>
                            </div>
                        </a>
                    </article>
                <% }); %>
            </div>
        <% } %>
    </main>

    <footer class="gallery-footer">
        <p>&copy; <%= new Date().getFullYear() %> Art Gallery. All rights reserved.</p>
    </footer>
</body>
</html>